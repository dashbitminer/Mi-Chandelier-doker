name: Deploy to Server via SSH

on:
  push:
    branches:
      - main  # Ejecutar el deploy cuando haya un push en la rama main

jobs:
  deploy:
    runs-on: ubuntu-22.04
  
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Download Tailscale
        run: |
          echo "🔹 [INFO] Instalando Tailscale..."
          curl -fsSL https://tailscale.com/install.sh | sh

      - name: Start Tailscale
        run: |
          echo "🔹 [INFO] Iniciando Tailscale..."
          sudo tailscale up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }}

      - name: Configure SSH Key and Connect
        env:
          PRIVATE_KEY: ${{ secrets.PRIVATE_KEY }}
          SERVER_IP: ${{ secrets.SERVER_IP }}
        run: |
          echo "🔹 [INFO] Verificando dirección del servidor..."
          if [[ -z "$SERVER_IP" ]]; then
            echo "❌ ERROR: SERVER_IP está vacío"
            exit 1
          fi

          echo "🔹 [INFO] Probando conexión con el servidor..."
          ping -c 3 "$SERVER_IP" || { echo "❌ ERROR: No se puede alcanzar $SERVER_IP"; exit 1; }

          echo "🔹 [INFO] Verificando estado de Tailscale..."
          sudo tailscale status || { echo "❌ ERROR: Tailscale no está funcionando"; exit 1; }

          echo "🔹 [INFO] Creando directorio SSH..."
          mkdir -p ~/.ssh  

          echo "🔹 [INFO] Guardando la clave privada..."
          echo "$PRIVATE_KEY" | awk 'BEGIN{RS="\\n";ORS="\n"}{print}' > ~/.ssh/id_rsa

          echo "🔹 [INFO] Ajustando permisos de la clave..."
          chmod 600 ~/.ssh/id_rsa  

          echo "🔹 [INFO] Verificando que la clave privada es válida..."
          if ! grep -q "PRIVATE KEY" ~/.ssh/id_rsa; then
            echo "❌ ERROR: La clave privada no tiene el formato correcto"
            exit 1
          fi

          echo "🔹 [INFO] Deteniendo cualquier agente SSH previo..."
          pkill ssh-agent || true

          echo "🔹 [INFO] Iniciando nuevo agente SSH..."
          eval "$(ssh-agent -s)"

          echo "🔹 [INFO] Agregando la clave privada al agente SSH..."
          ssh-add ~/.ssh/id_rsa || { echo "❌ ERROR: No se pudo agregar la clave SSH"; exit 1; }

          echo "🔹 [INFO] Agregando la IP del servidor a los hosts conocidos..."
          ssh-keyscan -H "$SERVER_IP" >> ~/.ssh/known_hosts || { echo "❌ ERROR: ssh-keyscan falló"; exit 1; }

          echo "🔹 [INFO] Verificando conexión SSH..."
          ssh -i ~/.ssh/id_rsa -o StrictHostKeyChecking=no ubuntu@$SERVER_IP "echo '✅ Conexión SSH exitosa'" || { echo "❌ ERROR: No se pudo conectar al servidor"; exit 1; }

      - name: Deploy code to server
        env:
          SERVER_IP: ${{ secrets.SERVER_IP }}
        run: |
          echo "🔹 [INFO] Conectando al servidor y desplegando código..."
          ssh -o StrictHostKeyChecking=no ubuntu@$SERVER_IP << 'EOF'
            echo "🔹 [INFO] Moviéndonos al directorio de la aplicación..."
            cd /var/www/fastuser/data/www/awschandelier.romeo-dev.online || { echo "❌ ERROR: No se pudo acceder al directorio"; exit 1; }

            echo "🔹 [INFO] Haciendo pull del código..."
            git pull origin main || { echo "❌ ERROR: git pull falló"; exit 1; }

            echo "🔹 [INFO] Reiniciando servicios..."
            sudo systemctl restart nginx || { echo "❌ ERROR: No se pudo reiniciar Nginx"; exit 1; }
            sudo systemctl restart php8.1-fpm || { echo "❌ ERROR: No se pudo reiniciar PHP-FPM"; exit 1; }

            echo "🔹 [INFO] Limpiando cachés de Laravel..."
            php artisan config:clear
            php artisan cache:clear
            php artisan route:clear
            php artisan view:clear

            echo "🔹 [INFO] Ejecutando migraciones..."
            php artisan migrate --force || { echo "❌ ERROR: No se pudieron ejecutar las migraciones"; exit 1; }

            echo "🔹 [INFO] Reiniciando colas de Laravel..."
            php artisan queue:restart || { echo "❌ ERROR: No se pudo reiniciar las colas"; exit 1; }

            echo "✅ [INFO] Despliegue completado exitosamente"
          EOF
